from PIL import Image
import pygame
import os


class Object:
     class Transform():
          name = ""
          active = True
          position = (0,0)
          rotation = (0)
          scale = (1,1)
          def __init__(self, name, active ,position, rotation, scale):
               #GetInfo
               self.name = name
               self.active = active
               self.position = position
               self.rotation = rotation
               self.scale = scale

     class SpriteRenderer():
          sprite = Image.new('RGBA', (1, 1))
          color = (255,255,255)
          def __init__(self, imagefile):
               #GetInfo
               if imagefile != "empty":
                    self.sprite = Image.open(imagefile)
               else:
                    self.sprite.save("emptySprite.png")
                    print("ERROR:" + "EMPTY FILE!!!")
